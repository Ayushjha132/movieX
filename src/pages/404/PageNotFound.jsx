import './style.scss';

const PageNotFound = () => {
    return (
        <div style={{height: window.innerHeight, color: "white", display: "flex", alignItems:"center",  justifyContent: "center"}}>
            Page Not Found
        </div>
    )
}

export default PageNotFound